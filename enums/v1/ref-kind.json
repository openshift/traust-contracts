{
  "name": "ref_kind",
  "source_schema": "report.schema.json#/properties/metadata/properties/ref_kind",
  "description": "Kind of git reference being audited (branch, tag, default branch, or dist-git stream).",
  "values": [
    "branch",
    "tag",
    "default",
    "stream"
  ],
  "note": "Shared across audit schemas; 'stream' added in harness v0.140.0 for dist-git release stream support",
  "used_in_schemas": [
    "report.schema.json",
    "cloud-config-audit.schema.json",
    "cloud-config-findings-current.schema.json",
    "isolation-review.schema.json",
    "vuln-findings.schema.json"
  ]
}
