{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/traust-contracts/schemas/v1/rpm-distgit-watch.schema.json",
  "title": "RPM dist-git Watch List",
  "description": "Schema for config/rpm-distgit-watch.yaml \u2014 operator-curated dist-git repositories to watch for the threat-model event feeder. Permissive by design: the file ships with an empty active list; each entry is an https dist-git URL string, and additional operator annotations are allowed.",
  "type": "object",
  "required": [
    "active"
  ],
  "additionalProperties": true,
  "properties": {
    "active": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  }
}
