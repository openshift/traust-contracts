{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/traust-contracts/schemas/v1/rule-pack-allowlist.schema.json",
  "title": "Rule Pack Allowlist",
  "description": "Schema for config/rule-pack-allowlist.yaml \u2014 supplemental external opengrep packs enabled per rule. Permissive by design: each pack carries an enabled flag, source pointer and per-rule / held_back lists whose shapes vary; only the top-level packs container is validated.",
  "type": "object",
  "required": [
    "packs"
  ],
  "additionalProperties": true,
  "properties": {
    "packs": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
          "enabled": {
            "type": "boolean"
          },
          "source": {
            "type": "string"
          },
          "rules": {
            "type": "array"
          }
        }
      }
    }
  }
}
