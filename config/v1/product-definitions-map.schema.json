{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/traust-contracts/schemas/v1/product-definitions-map.schema.json",
  "title": "Product Definitions Map",
  "description": "Schema for config/product-definitions-map.yaml \u2014 reviewed findings-package/repo -> ps_product mappings (human-asserted). Permissive by design: the mappings container carries packages and repos maps whose keys and values are deployment-specific; only the top-level shape is validated.",
  "type": "object",
  "required": [
    "mappings"
  ],
  "additionalProperties": true,
  "properties": {
    "mappings": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "packages": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "repos": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        }
      }
    }
  }
}
